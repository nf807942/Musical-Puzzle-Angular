import { AudioPuzzleManager } from './audio-puzzle-manager';

describe('AudioPuzzleManager', () => {
  it('should create an instance', () => {
    expect(new AudioPuzzleManager()).toBeTruthy();
  });
});
